name: 📄 Resume PDF (ATS — No Photo)

on:
  push:
    paths:
      - 'scripts/resume.html'
      - 'scripts/generate-pdf.js'
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-pdf:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate Profile.pdf
        run: node scripts/generate-pdf.js

      - name: Upload PDF artifact
        uses: actions/upload-artifact@v4
        with:
          name: Profile-ATS
          path: public/assets/Profile.pdf
          retention-days: 90

      - name: Check for changes
        id: diff
        run: |
          git diff --quiet public/assets/Profile.pdf && echo "changed=false" >> "$GITHUB_OUTPUT" || echo "changed=true" >> "$GITHUB_OUTPUT"

      - name: Commit & push updated PDF
        if: steps.diff.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/assets/Profile.pdf
          git commit -m "chore: auto-regenerate Profile.pdf [skip ci]"
          git push
